displayName: Header To Query
type: middleware
iconPath: .assets/icon.png

import: github.com/zalbiraw/headertoquery

summary: 'Converts HTML headers to URL query parameters'

testData:
  Headers:
    - name: SERVICE-TAG
      key: id
    - name: RANK
    - name: GROUP
      keepHeader: true